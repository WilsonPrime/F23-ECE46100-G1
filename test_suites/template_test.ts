//test suite template, use for any functions that need to be tested
/*
import { function } from './filename';

describe('function name', () => {
    it('describe expected result 1 given input', async () => {
      const result1 = await function(input); //only if async function, otherwise can skip await call
      expect(result1).toBe(expected result1);
    });
  
    it('describe expected result 2 given input', async () => {
      const result2 = await function(input); //only if async function, otherwise can skip await call
      expect(result2).toBe(expected result2);
    });
});

--repeat for all functionalities that need to be tested individually
*/